// Age

let age = 20;

let ageResult = age >= 18 ? "Adult" : "Minor";

console.log(ageResult);


// Pass / Fail

let marks = 75;

let result = marks >= 40 ? "Pass" : "Fail";

console.log(result);


// Even / Odd

let number = 10;

let numberType = number % 2 === 0 ? "Even" : "Odd";

console.log(numberType);


// Login

let isLoggedIn = true;

let message = isLoggedIn ? "Welcome" : "Please Login";

console.log(message);


// Discount

let amount = 1500;

let discountMessage =
    amount >= 1000 ? "Discount Applied" : "No Discount";

console.log(discountMessage);